---
description: SmartCRM — как выполнять задачи PRD_MAP (апрув, тесты, ops, коммиты)
alwaysApply: true
---

# SmartCRM — workflow задач

Канон процесса для пунктов **PRD_MAP**, фич и фиксов в репозитории.

---

## Старт

- Ждать **явный пункт** (PRD_MAP / задача) + **что сделать**.
- Перед работой — пробежать **`docs/operations/BACKLOG.md`** на хвост, привязанный к этому пункту MAP.
- Показать **план на 1 шаг** (файлы, тесты, риски) → **`go`** → работа.
- Без `go` — код не трогать.

---

## Тесты (обязательно, без исключений)

- У **каждой** новой или изменённой фичи, файла, функции, роута, скрипта — **свой тест** в том же шаге.
- Порядок: **тест под задачу** → код → **регрессия блока** (pytest + смоук; UI — Chrome DevTools MCP / чеклист).
- Моки, fixtures, smoke-данные — по необходимости.
- Красные тесты → запись в **`docs/operations/ISSUES.md`** → фикс (после `go`) → снова тесты. Шаг не закрывать без зелёного.

---

## Финиш шага (всегда, даже если пользователь не написал «коммит» / «опс»)

- **Коммит** после зелёных тестов.
- **`docs/operations/SESSION_STATE.md`** — после каждого действия.
- **`CHANGELOG` + `HANDOFF`** — батч в конце логического шага.
- **`ISSUES`** — при любом баге (до и после фикса).
- **`BACKLOG`** — при отложенном scope (см. § Бэклог).
- **`backend/**/README.md`** — если менялся `backend/` (см. §5 в `smartcrm-repo-layout.mdc`).
- **`[x]` в PRD_MAP** — только после явного **«ок»** пользователя.

---

## Только с явным апрувом пользователя

- `git push`
- deploy и любые прод-действия
- деструктивное (DROP, reset, force push, удаление файлов проекта)
- смена scope / PRD без решения пользователя

---

## Один шаг = один пункт

- Не пакетить несколько пунктов PRD_MAP в один проход без отдельного `go`.

---

## Честный отчёт (anti-hallucination, обязательно)

**Не врать о результате:** лучше честный хвост, чем ложный done.

После **каждого** шага в отчёте пользователю — секция **«Не сделано / не проверено»**:

| Что писать | Пример |
|------------|--------|
| Из плана не выполнено | «README зоны не обновлён» |
| Не запускал | «pytest не гонял — нужно проверить» |
| Не читал / не открывал | «файл X не читал, утверждение по памяти» |
| Додумано | «Assumption: … — не подтверждено в коде/логах» |

**Запрещено** без факта: «готово», «всё работает», «N passed», «проверил UI», «деплой ок».

**Done** шага только если: тесты зелёные **с выводом команды** **или** статус **blocked** + что осталось.

Формат отчёта шага:

1. Пункт / что сделано  
2. Тесты (команда + результат или «не запускал»)  
3. **Не сделано / не проверено**  
4. **BACKLOG:** что добавлено в `docs/operations/BACKLOG.md` (или «ничего») + **хвост** — 3–5 верхних пунктов по приоритету  
5. Статус: `done` | `blocked` | `in_progress`

---

## Бэклог и техдолг (обязательно)

**Куда:** `docs/operations/BACKLOG.md` (не ISSUES — там только баги).

**Когда писать:** отложили часть работы из текущего шага — **сразу** строка в BACKLOG, в том же коммите что и шаг.

**Связь с PRD (обязательно):**

- Поле **`PRD_MAP`** — **точный пункт** чеклиста из [`docs/product/PRD_MAP.md`](../../docs/product/PRD_MAP.md) (цитата или однозначная подсекция + пункт).
- **Не** складывать всё в «Фаза 3» без пункта MAP.
- **`Нужна фаза MAP`** (1 / 2 / 3 / —) — **опционально**: только если понятно, на каком этапе PRD_MAP этот хвост реально закрывать.

**Запрещено:** закрыть шаг с отложенным scope без записи в BACKLOG.

Формат записи:

`[дата] | PRD_MAP: «…» | Отложено: … | Причина: … | Нужна фаза MAP: … | Из шага: …`
